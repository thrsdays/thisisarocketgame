using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[ExecuteInEditMode]
public class LaunchPadPositioning : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("spa");
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("spasssd");
    }
}
